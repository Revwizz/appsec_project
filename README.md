Go to final_project directory in cmd

IN CMD (Below)

1. Type "npm install" in the final_project directory.

2. Once all the package is download, type "npm start" to run the server.

3. Open the browser and Copy and Paste "http://localhost:3000" to the URL.

4. Once you played enough, Press the "ctrl + c" to quit the server.
